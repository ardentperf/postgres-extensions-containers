# {{ replaceAll .Name "-" "_" }}
<!--
SPDX-FileCopyrightText: Copyright © contributors to CNPG Extensions.
SPDX-License-Identifier: Apache-2.0
-->

<!--
TODO: Replace this section with a brief introduction of the extension.
Describe what it does, link to the upstream project, and state the supported
PostgreSQL versions and Debian distributions.
-->

[{{ replaceAll .Name "-" "_" }}](https://github.com/<owner>/<repo>) is a
PostgreSQL extension that provides [describe the main functionality here].

## Usage

<!--
TODO: Customize the image tag, extension names, SQL version, and Renovate
instructions below. Use the package version from `metadata.hcl` for the image
tag. The Database version is the SQL extension version and may omit a leading
`v`.
-->

### 1. Add the {{ .Name }} extension image to your Cluster

Define the `{{ .Name }}` extension under the `postgresql.extensions`
section of your `Cluster` resource. For example:

```yaml
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cluster-{{ .Name }}
spec:
  imageName: ghcr.io/cloudnative-pg/postgresql:{{ .DefaultVersion }}-minimal-{{ .DefaultDistro }}
  instances: 1

  storage:
    size: 1Gi

  postgresql:
    extensions:
    - name: {{ .Name }}
      image:
        # TODO: Keep this image tag synchronized with metadata.hcl.
        reference: ghcr.io/cnpg-extensions/{{ .Name }}:<package-version>-{{ .DefaultVersion }}-{{ .DefaultDistro }}
```

### 2. Enable the extension in a database

You can install `{{ replaceAll .Name "-" "_" }}` in a specific database by creating or
updating a `Database` resource, or by running `CREATE EXTENSION` directly in
`psql`. For example, to enable it in the `app` database:

```yaml
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: cluster-{{ .Name }}-app
spec:
  name: app
  owner: app
  cluster:
    name: cluster-{{ .Name }}
  extensions:
  - name: {{ .Name }}
    # TODO: Set this to the extension's SQL default_version.
    version: '<sql-version>'
```

Alternatively, you can enable the extension directly with SQL:

```sql
CREATE EXTENSION {{ replaceAll .Name "-" "_" }};
```

### 3. Verify installation

Once the database is ready, connect to it with `psql` and run:

```sql
\dx
```

You should see `{{ replaceAll .Name "-" "_" }}` listed among the installed extensions.

<!--
TODO: Add a Known Caveats section if the extension requires shared preload
libraries, PostgreSQL parameters, runtime libraries, or special SQL setup.
-->

## Contributors

This extension is maintained by:

- FirstName LastName (@GitHub_Handle)

The maintainers are responsible for:

- Monitoring upstream releases and security vulnerabilities.
- Ensuring compatibility with supported PostgreSQL versions.
- Reviewing and merging contributions specific to this extension's container
  image and lifecycle.

---

## Licenses and Copyright

This container image contains software that may be licensed under various
open-source licenses.

All relevant license and copyright information for the `{{ replaceAll .Name "-" "_" }}`
extension and its dependencies are bundled within the image at:

```text
/licenses/
```

By using this image, you agree to comply with the terms of the licenses
contained therein.
