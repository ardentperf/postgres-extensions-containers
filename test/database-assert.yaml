apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: (join('-', [$values.image_name, 'app']))
status:
  applied: true
  extensions:
  - applied: true
    name: ($values.sql_name)
  observedGeneration: 1
